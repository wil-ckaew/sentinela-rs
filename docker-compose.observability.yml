services:
  prometheus:
    image: prom/prometheus
    volumes:
      - ./k8s/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    ports:
      - "3001:3000"
