apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sentinela-ingress
spec:
  rules:
    - host: sentinela.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
